[build]
builder = "NIXPACKS"
nixPackagesPath = "backend"
buildCommand = "cd backend && poetry install --no-root"

[deploy]
startCommand = "cd backend && poetry run uvicorn app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
