[build]
builder = "NIXPACKS"
buildCommand = "cd backend && pip install poetry && poetry install --no-root"

[deploy]
startCommand = "cd backend && poetry run uvicorn app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
